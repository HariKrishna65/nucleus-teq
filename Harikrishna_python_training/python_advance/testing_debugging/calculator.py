"""
Write pytest test cases for a function that adds two numbers.
"""
def add(
    first_number: int,
    second_number: int
) -> int:

    return (
        first_number
        + second_number
    )
