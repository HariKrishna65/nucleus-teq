$kind: http-request
method: POST
url: '{{base_url}}/auth/verify'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "token": "sample-verification-token"
    }
